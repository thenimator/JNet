#pragma once

namespace JNet {
    namespace udp {
        enum class receiveMode {
            queue,
            callback
        };
    }
    namespace tcp {
        enum class receiveMode {
            queue,
            callback
        };
    }
        
    
}